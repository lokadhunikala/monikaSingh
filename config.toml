# Main settings
baseURL = 'https://github.com/lokadhunikala/monikaSingh/'
title = 'Monika Singh Gallery'

# FIX 1: Updated from languageCode to locale to fix the deprecation warning
locale = 'en-us'

# FIX 2: Configured explicitly as a Hugo Module (this skips the themes folder check)
[module]
  [[module.imports]]
    path = 'github.com/bep/gallerydeluxe'